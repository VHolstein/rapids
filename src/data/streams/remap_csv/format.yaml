PHONE_LOCATIONS:
  ANDROID:
    RAPIDS_COLUMN_MAPPINGS:
      TIMESTAMP: timestamp
      DEVICE_ID: device_id
      LATITUDE: latitude
      LONGITUDE: longitude
      SPEED: speed
      ALTITUDE: altitude
      ACCURACY: accuracy
    MUTATION:
      COLUMN_MAPPINGS:
      SCRIPTS: # List any python or r scripts that mutate your raw data
  IOS:
    RAPIDS_COLUMN_MAPPINGS:
      TIMESTAMP: timestamp
      DEVICE_ID: device_id
      LATITUDE: latitude
      LONGITUDE: longitude
      SPEED: speed
      ALTITUDE: altitude
      ACCURACY: accuracy
    MUTATION:
      COLUMN_MAPPINGS:
      SCRIPTS: # List any python or r scripts that mutate your raw data
